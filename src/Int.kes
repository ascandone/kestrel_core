extern pub type Int

extern pub let (+): Fn(Int, Int) -> Int

extern pub let (-): Fn(Int, Int) -> Int

extern pub let (*): Fn(Int, Int) -> Int

extern pub let (/): Fn(Int, Int) -> Int

extern pub let (^): Fn(Int, Int) -> Int

extern pub let (%): Fn(Int, Int) -> Int
