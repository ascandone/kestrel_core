pub(..) type Tuple2<a, b> {
  Tuple2(a, b),
}

pub let first = fn tuple2 {
  match tuple2 {
    (x, _) => x,
  }
}

pub let second = fn tuple2 {
  match tuple2 {
    (_, x) => x,
  }
}

pub(..) type Tuple3<a, b, c> {
  Tuple3(a, b, c),
}

pub(..) type Tuple4<a, b, c, d> {
  Tuple4(a, b, c, d),
}
